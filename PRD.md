### 产品需求文档（PRD）——Web 版 AI 旅行规划师（AI Travel Planner）

---

#### 1. 产品概述
- **目标**：通过语音/文字输入快速采集用户出行需求，借助大语言模型生成个性化行程方案，并提供费用预算与出行辅助能力。
- **价值**：降低旅行规划门槛与时间成本；提供可执行的交通/住宿/景点/餐饮清单与预算建议；多端同步，便于随时查看与调整。
- **平台**：Web（PC 与移动端浏览器）。

#### 2. 目标用户与使用场景
- **目标用户**：
  - 自助行用户（个人/家庭/朋友结伴）
  - 预算敏感型用户（希望事前预估开销）
  - 时间紧张的上班族（希望快速得到可执行方案）
- **常见场景**：
  - “我想去日本，5 天，预算 1 万元，喜欢美食和动漫，带孩子”
  - 出发前/旅途中随时调整日程与预算
  - 多设备（家中电脑、手机）查看与编辑

#### 3. 目标与非目标
- **必须达成**：
  - 语音与文字输入需求采集
  - 自动生成行程（交通/住宿/景点/餐饮/每日计划）
  - 费用预算估算与开销记录
  - 账号体系与云端同步
  - 地图位置与导航能力
- **暂不包含（可选/后续）**：
  - 机票/酒店直连预订与支付
  - 社交分享、多人协同编辑

#### 4. 关键用户故事（验收标准由下文功能验收细化）
1) 作为用户，我可以用语音说出目的地/日期/预算/偏好，系统自动生成行程方案。
2) 作为用户，我可以查看每日计划、交通与住宿建议，并在地图上查看位置。
3) 作为用户，我可以记录与查看开销，系统给出预算对比与提醒。
4) 作为用户，我可以注册登录，并在不同设备上访问同一行程。

#### 5. 功能需求
- **5.1 需求采集（语音/文本）**
  - 语音转文字：支持中文为主；可扩展多语言。
  - 表单要素：目的地、日期范围、天数、预算、同行人数、偏好（美食/亲子/动漫/自然/文化等）。
  - 历史输入管理：可从历史记录快速复用。
- **5.2 行程生成（LLM 驱动）**
  - 生成内容：
    - 总览：行程摘要、每日主题、总预算估算
    - 每日：时间轴（日程）、景点清单、就餐建议、交通建议（城际/市内）、住宿建议
    - 约束：遵循预算、偏好、同行人特征（如亲子）
  - 可编辑：用户可插入/删除/调整每日项，触发“局部再生成/整体微调”。
- **5.3 费用预算与管理**
  - 预算面板：机酒、交通、餐饮、门票、杂项分类预算与估算区间
  - 开销记录：手动添加（支持语音录入），按分类统计，显示与预算对比
  - 提醒：超预算时提醒；展示节省建议
- **5.4 地图与导航**
  - 地图展示：景点/住宿/餐饮点位标注；按日视图聚合
  - 导航：跳转第三方地图导航；支持位置查询与附近探索
- **5.5 用户与数据**
  - 注册/登录：邮箱/第三方（可选）
  - 行程管理：多份行程的创建、复制、归档、删除
  - 云端同步：跨设备查看与编辑
- **5.6 设置与密钥管理**
  - 在设置页输入/管理第三方 API Key（绝不写入仓库）
  - 本地安全存储与云端机密配置（依据所选后端能力）

#### 6. 信息架构与主要页面
- 登录/注册
- 行程列表页（最近使用置顶、搜索）
- 新建行程向导（语音/文字输入 + 表单补充）
- 行程详情页：
  - 顶部摘要与预算概览
  - 日期切换与时间轴
  - 地图视图（按日筛选）
  - 费用记录/统计
  - 再生成/微调入口
- 设置页：API Key、语音/地图/LLM 提供商配置

#### 7. 交互与流程（高层）
1) 采集需求 → 调用语音识别（可选）→ 生成结构化参数
2) 调用 LLM 生成行程初稿 → 前端展示摘要与每日卡片
3) 用户编辑/微调 → 局部再生成或整体刷新
4) 地图渲染点位 → 支持跳转导航
5) 记录开销 → 预算对比与提醒
6) 自动保存至云端 → 多设备同步

#### 8. 技术方案建议（可替换，非强制）
- **语音识别**：科大讯飞、阿里云智能语音、或其他同类服务
- **地图**：高德地图或百度地图 Web API
- **后端与认证/数据库**：
  - Supabase（Auth + Postgres + 存储）
  - 或 Firebase Authentication + Firestore
  - 或自建后端（Node.js/Go 等）+ 托管 Postgres
- **LLM**：可接入阿里云百炼/OpenAI/智谱/DeepSeek 等，抽象统一接口
- **前端**：React/Next.js 或 Vue/Nuxt；Ant Design/Tailwind 作为 UI 方案
- **部署**：Docker 容器化；可用 GitHub Actions 构建并推送镜像至阿里云镜像仓库

#### 9. 外部 API 与集成点
- 语音识别：开始/停止录音、上传音频、识别结果回调
- 地图：地点搜索、坐标/逆地理、点位标注、规划（跳转导航）
- LLM：行程生成/微调、预算估算、要点摘要
- 身份认证：注册/登录/刷新令牌/登出
- 存储：行程、偏好、费用记录、点位缓存

#### 10. 数据模型（示例）
- User：id，email，displayName，createdAt
- Trip：id，userId，title，destination，startDate，endDate，days，budget
- Preference：tripId，tags[string]（美食/亲子/动漫/...）
- DayPlan：id，tripId，dayIndex，items[PlanItem]
- PlanItem：timeRange，type（spot/food/hotel/transport），name，address，geo{lat,lng}，notes，costEst
- Expense：id，tripId，date，category，amount，note

#### 11. 权限与安全
- 行程/费用仅属当前用户可见（行级权限）
- 认证令牌安全存储（HttpOnly Cookie 或安全存储）
- 严禁在代码库中包含任何密钥；密钥由设置页输入，或在服务器侧通过安全配置管理

#### 12. 非功能需求
- 性能：
  - 首屏加载 < 3s（典型网络）
  - 行程生成请求超时/重试策略（前端 30~60s 超时提示）
- 可用性：
  - 移动端适配
  - 语音输入失败时可回退到文本表单
- 可靠性：
  - 自动保存与版本化（至少保留最近 5 个版本快照）
- 隐私与合规：
  - 明确告知数据用途；可删除账号与数据

#### 13. 里程碑与范围
- M1 可用原型（3-5 天）
  - 文字输入 → 生成行程（摘要+每日）
  - 行程详情展示 + 简单编辑
  - 基础费用面板（估算只读）
- M2 语音与地图（1-2 周）
  - 语音转文字采集需求
  - 高德/百度地图点位展示与筛选
  - 开销记录 + 预算对比
- M3 账号与同步（1 周）
  - 注册登录、行程云端存储
  - 多端同步
- M4 打包与发布（1 周）
  - Docker 化，README，在线镜像
  - 可选：GitHub Actions → 阿里云镜像仓库

#### 14. 验收标准（关键用例）
1) 可在设置页配置 LLM/语音/地图的 API Key；未配置前给出引导，不阻塞纯浏览。
2) 用户输入（语音/文字）后，生成包含每日行程卡片的方案，含交通/住宿/景点/餐饮建议与预算估算。
3) 地图可按日显示对应点位，并可跳转第三方导航。
4) 可新增一条开销记录，预算对比图表实时更新。
5) 注册登录成功后，新建行程自动保存并可在另一设备查看与编辑。

#### 15. 发布与交付要求（对齐原始要求）
- 提交一个 PDF 文件，包含 GitHub 仓库地址与 README 链接/内容。
- 代码提交至 GitHub，并提供可直接下载运行的 Docker 镜像与运行说明。
- 若未使用阿里云百炼的 Key，请在 README 中提供可用 3 个月的 Key（仅用于助教批改）。
- 建议通过 GitHub Actions 打包 Docker 镜像并推送至阿里云镜像仓库（可选）。
- 再次强调：切勿在公开仓库中硬编码任何密钥。

#### 16. 风险与对策
- 第三方 API 配额/费用波动 → 可切换多家供应商；本地缓存/降级策略
- LLM 结果稳定性差 → 模板化提示词 + 结构化后处理 + 人工可编辑
- 地图合规与出海问题 → 按地域选择合适地图供应商与合规配置
- 隐私与合规风险 → 最小化采集、明示告知、便捷数据删除

#### 17. 关键指标（KPI）
- 首次行程生成用时中位数（目标 < 20 秒，视模型而定）
- 成功完成一次出行规划的用户占比（创建→查看→保存）
- 预算记录功能的使用率与回访率
- 行程编辑后再生成的满意度（反馈问卷/简评）

#### 18. 决策基线（默认推荐）
- 技术选型
  - 语音识别：科大讯飞
  - 地图服务：高德（海外目的地场景预留 Google/Mapbox 备用）
  - 后端与数据平台：Supabase（Auth + Postgres + 存储）
  - 大模型供应商：阿里云百炼（抽象接口以便切换 OpenAI/智谱/DeepSeek）
  - 前端技术栈：React/Next.js + Ant Design
  - 部署与 CI/CD：GitHub Actions（打包 Docker 镜像并可推送至阿里云镜像仓库）
- 范围与优先级
  - 首发不含机票/酒店直连预订与支付
  - 分享能力先支持“只读链接”，不做多人协同编辑
  - 里程碑：M1 文字生成功能 → M2 语音+地图 → M3 账号与同步 → M4 打包发布
- 行程生成策略
  - 时间粒度：按天 + 关键时段
  - 再生成模式：局部微调优先，保留全量重算入口
  - 预算约束：混合（以提示为主，必要时提供替换建议）
- 费用与预算
  - 分类：机酒、交通、餐饮、门票、杂项
  - 币种：单币种 RMB（后续支持多币种与汇率）
  - 超预算提醒阈值：10%
- 地图与导航
  - 导航策略：跳转第三方导航（高德/百度 App 或 Web）
  - 点位来源：LLM 建议 + 地图 POI 搜索（去重与消歧）
  - 海外可用性：按目的地/网络判断并启用备用供应商
- 账号与数据
  - 登录方式：邮箱优先，可扩展第三方 OAuth
  - 分享权限：只读链接
  - 版本化：最近 5 个快照版本
- 配置与密钥
  - Key 存储策略：混合（前端仅存可暴露的 Key；服务端安全托管）
  - 环境区分：开发/测试/生产分离，使用 .env 管理
- 非功能与合规
  - 性能：首屏 < 3s；生成请求超时 60s；懒加载
  - 观测：前端埋点 + 错误上报
  - 隐私合规：最小化采集、数据可删除、清晰提示告知
- 交付与验收
  - PDF 生成：手工整理 + 脚本模板
  - Docker 基础镜像：node:18-alpine
  - README 指引：本地运行与 Docker 运行双方案（示例 Key 仅用于评审）

---

本 PRD 基于 `raw_requirements.txt` 归纳与细化，后续可在实现阶段进一步拆分为任务与技术设计文档（API 规格、数据表结构与权限策略等）。


